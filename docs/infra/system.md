# 전월세 부동산 금융 플랫폼 — 시스템 구성서

> 노드 구성 · 가용성 목표 · 비용
> 논리 구성도 · RTO/RPO · 월 비용 산정
> ※ 각 구성 요소가 왜 필요한지는 `docs/features/infra.md`를 따른다.
> 작성 기준일 : 2026년 8월

## 1. 문서 목적

본 문서는 `docs/features/infra.md`가 정의한 인프라 기능(INF-01 ~ 04 · 07 ~ 09. INF-05 · 06은 차기 범위)이 **실제로 어디에 배치되고, 어떤 목표를 만족해야 하며, 얼마의 비용이 드는지**를 정의한다.

기능이 무엇을 제공하고 왜 필요한지는 `docs/features/infra.md`가 다룬다. 본 문서는 그 기능들이 놓이는 물리적·논리적 구성과 수치 목표만 담는다.

---

## 2. 논리 구성도

논리 구성도: `docs/infra/systemdiagram.png`
APP-01 · DB-01 · DB-02 세 노드의 구성과 상시 경로(그림 기준). Rocky 이전 뒤 NAS-01 · NAT-01이 더해져 다섯 노드다 — 아래 표.

| 노드 | 담긴 것 |
|---|---|
| APP-01 | Nginx, 애플리케이션 두 슬롯, 정적 화면(web), Redis. 1차 배포에서는 PostgreSQL도 함께 둔다(2.1절) |
| DB-01 | PostgreSQL Primary. WAL을 오브젝트 스토리지로 아카이빙 |
| DB-02 | PostgreSQL Standby. 복제만 수신하며 트래픽을 받지 않는다. DB-01과 다른 가용 영역 |
| NAS-01 | NFS 서버 — 백업 저장소(INF-08). 사설 서브넷 |
| NAT-01 | NAT 인스턴스 — 사설 서브넷의 인터넷 출구(INF-07). 공개 서브넷 |

**NAS-01 · NAT-01과 서브넷 구성은 Rocky Linux 이전 뒤 더하는 서버 운영 기반이다**(INF-07 ~ 09, 2026-09-19). 네트워크 격리 · 파일시스템 · 계정 · 정기 작업의 설계와 근거는 서버 운영 기반 설계서(`docs/infra/platform.md`)가 갖는다. 논리 구성도 그림(`systemdiagram.png`)에는 아직 없다 — 그림을 고칠 때 함께 넣는다.

**관측 스택(INF-05)과 부하 생성기 APP-02(INF-06)는 차기 범위다**(2026-09-19 결정). 논리 구성도 그림에는 아직 그려져 있다 — 그림을 고칠 때 함께 뺀다.

### 2.1 배치 대상

| 단계 | 대상 | OS | 시점 |
|---|---|---|---|
| 1차 | AWS EC2 | Amazon Linux 2023 — EC2 기본 AMI | 로드맵 4주차 |
| 2차 | AWS EC2 | **Rocky Linux 9** — Rocky 공식 AMI(AWS Marketplace) | 로드맵 6주차 |

- **1차 배포는 APP-01 한 대에 PostgreSQL까지 둔다.** DB 노드 분리 · 복제(INF-03)에서 DB-01 · DB-02로 옮긴다 — 로드맵 6주차. 구성 파일은 `infra/docker-compose.yml`.
- **1차에서 구성을 완성하고 2차에서 같은 구성을 옮긴다.** 컨테이너로 구동하므로 옮기는 대상은 OS 준비(Docker 설치 · SELinux · 방화벽)와 데이터다. 애플리케이션 이미지와 Compose 파일은 그대로다.
- Rocky Linux 9를 고른 이유: 공식 AMI가 Marketplace에 있고 Docker 공식 저장소가 RHEL 9 계열을 지원한다. 10은 2025년 공개로 운용 사례가 적다. **부 버전은 AMI를 받을 때 확정해 여기에 적는다.**
- **접속은 공인 IP + HTTP다.** 도메인 · TLS는 두지 않는다(2026-09-19 결정). 4장의 443 행과 6장의 TLS 인증서 · 도메인 행은 도메인을 붙일 때 되살린다.
- 인스턴스 유형 · 리전 · 계정은 **미확정** — AWS 계정 정보를 받은 뒤 1차 배포 작업에서 확정해 이 표에 적는다.

## 3. 노드 구성

| 노드 | 사양 | 구동 대상 | 상시 기동 |
|---|---|---|---|
| APP-01 | **2 vCPU / 2 GB — t3.small (2026-09-23 확정)** | Nginx, App ×2, web, Redis. 1차 배포에서는 PostgreSQL도(2.1절) | 예 |
| DB-01 | 2 vCPU / 2 GB | PostgreSQL Primary | 예 |
| DB-02 | 1 vCPU / 1 GB | PostgreSQL Standby | 예 (복제 수신 전용) |
| NAS-01 | 미확정 | NFS 서버(백업 저장소) | 예 |
| NAT-01 | 미확정 — 최소 사양 | NAT(IP 포워딩 · 마스커레이드) | 예 |

**APP-01만 확정이고 나머지는 미확정이다.** APP-01은 사용자 결정으로 t3.small에 고정한다(2026-09-23). 초기 배치의 4 GB가 아니라 **2 GB**이며, 그 위에 앱 2슬롯 · Redis · 1차 배포의 PostgreSQL까지 올라가므로 **컨테이너마다 상한을 두지 않으면 커널이 메모리를 가장 많이 쓰는 프로세스를 정지시킨다.** 아래 자원 상한이 선택이 아니라 전제인 이유다. 나머지 노드는 부하 시험이 차기 범위로 빠졌으므로 구축 시점에 확정한다. DB-02는 승격 시 primary 역할을 감당하는지 확인이 필요하다(`docs/infra/runbook.md` 6.2절).

**자원 상한**

| 대상 | 설정 |
|---|---|
| 컨테이너 메모리 | 서비스마다 `mem_limit`. 상한의 합이 노드 메모리에서 OS · dockerd 몫을 뺀 값을 넘지 않는다 |
| JVM 힙 | 컨테이너 상한에 대한 비율로 준다. 상한이 없으면 JVM이 **호스트 전체**를 기준으로 힙을 잡는다 |
| PostgreSQL | `shared_buffers`, 최대 커넥션 수 상한 |
| Redis | `maxmemory` 상한과 축출 정책(`maxmemory-policy`). 상한이 없으면 APP-01의 다른 프로세스를 밀어낸다 |

**값은 운영 Compose(`infra/docker-compose.yml`)가 갖는다.** 여기에 옮겨 적지 않는다 — 두 곳에 적으면 한쪽만 고쳐진다. 값의 산정 근거는 그 파일의 주석에 있다.

**그 값들은 잠정이다.** 부하 시험이 차기 범위로 빠져 실측 근거가 없고, 노드 메모리에서 역산한 배분이다. **1차 배포에서 기동 후 실 사용량을 보고 확정한다** — 확정 시점은 서버 운영 기반 설계서 10장에 둔다. 컨테이너 메모리 상한은 JVM의 힙 크기뿐 아니라 **GC 선택까지 바꾼다**(server-class 판정이 호스트가 아니라 상한을 본다). 실측 때 함께 본다.

**Redis에는 복제도 백업도 두지 않는다.** 재생성 가능한 데이터만 보관한다. 다만 **내구성과 재시작 생존은 다른 문제다.** 리프레시 토큰이 Redis에 있어 컨테이너가 재생성되는 것만으로 접속 중인 전 사용자가 로그아웃된다 — 노드 재부팅 · 이미지 갱신 · OOM 어느 것으로도 일어난다. 그래서 **명명 볼륨과 추가 전용 파일(AOF)로 재시작을 견디게 한다.** 이것은 백업이 아니다. 노드가 사라지면 함께 사라지며, 그 경우는 여전히 재로그인으로 복구한다(5.2절).

APP-01 단일 노드에 두 프로세스를 두는 구성은 노드 자체의 장애에 대응하지 못한다. 비용 제약을 수용한 결과이며, 필요한 시점에 애플리케이션 노드를 하나 더 두고 upstream에 추가하는 것으로 확장한다. **본 설계의 목표는 구성을 완성하는 것이 아니라 확장 지점을 미리 정의하는 것이다.**

---

## 4. 네트워크 및 접근 통제

노드는 두 종류의 서브넷에 놓인다 — **공개 서브넷: APP-01 · NAT-01, 사설 서브넷: DB-01 · NAS-01(AZ-a), DB-02(AZ-c).** 사설 서브넷의 노드는 공인 주소가 없다. VPC · 서브넷 CIDR은 **미확정**이며 VPC 생성 작업에서 이 절에 적는다. 구성 근거는 서버 운영 기반 설계서 3장.

| 구간 | 포트 | 허용 범위 |
|---|---|---|
| 사용자 → Nginx | 80 | 전체. 도메인 · TLS를 붙이면 443을 열고 80은 443으로 리다이렉트한다(2.1절) |
| Nginx → App | 8081 · 8082 | 동일 노드 루프백 |
| Nginx → web(정적 화면) | 8090 | 동일 노드 루프백 |
| App → PostgreSQL | 5432 | APP-01 사설 IP만 |
| App → Redis | 6379 | 동일 노드 루프백. 외부 바인딩 금지 |
| Primary → Standby | 5432 | DB-01 사설 IP만 |
| 운영자 → APP-01 | 22 | 운영자 IP만. 공개키 인증만. 비밀번호 인증과 root 로그인은 차단 |
| APP-01 → 다른 노드(DB-01 · DB-02 · NAS-01 · NAT-01) | 22 | APP-01 사설 IP만 — 운영자는 APP-01을 경유(ProxyJump)해 들어간다 |
| DB-01 · DB-02 · APP-01 → NAS-01 | 2049 | 해당 노드 사설 IP만. NFSv4만 쓰므로 이 포트 하나 |
| 사설 서브넷 → NAT-01 | 전체(나가는 방향) | 사설 서브넷 CIDR만. 인터넷에서 NAT-01로 들어오는 길은 없다 |
| **APP-01 → 인터넷(나가는 방향)** | 전체(나가는 방향) | **전체 허용을 선택한다.** 외부 연동 API · 컨테이너 이미지 저장소 · 오브젝트 스토리지 · 메일 발송의 목적지가 전부 가변 IP라 목록으로 좁힐 수 없다. 좁히지 못하는 것과 정의하지 않은 것은 다르므로 여기에 적는다 — 침해 시 반출 경로가 열려 있다는 뜻이고, 그것을 받아들인 선택이다 |
| NAT-01 → 인터넷(나가는 방향) | 전체(나가는 방향) | 사설 노드의 패키지 갱신 · 이미지 수신이 여기로 나간다. 위와 같은 이유로 좁히지 않는다 |
| DB 노드 · NAS-01 → S3 | 443 | S3 게이트웨이 엔드포인트 — 인터넷 · NAT를 거치지 않는다. 같은 리전의 버킷에만 쓸 수 있다 |

**접근 통제는 겹으로 쌓는다** — 보안 그룹(1차) · firewalld(호스트) · 컨테이너 포트의 루프백 바인딩. Docker가 게시한 포트는 firewalld를 우회하므로 셋째 겹이 필요하다. **DB 노드의 5432는 예외로 두 겹**(보안 그룹 · 사설 서브넷)이다. **겹의 수는 구간마다 다르고 인스턴스 메타데이터가 별도의 면으로 붙는다** — 어느 구간이 몇 겹인지는 서버 운영 기반 설계서 3.3이 갖는다.

**공개된 포트는 자동 스캔의 대상이 된다.** 비밀번호 인증을 켜두면 대입 시도가 계속되고, Redis는 기본 설정에 인증이 없어 노출 시 즉시 접근된다. 따라서 SSH는 공개키 인증만 허용하고, 데이터 저장소는 인터넷에 노출하지 않는다.

**시크릿 주입 방식은 미확정이다.** DB 비밀번호, 외부 API 키, JWT 서명 키, 백업 복호화 키를(Slack 웹훅 · Grafana 계정은 관측이 차기 범위라 지금은 없다) 어디에 두고 어떻게 주입할지는 1주차 프로젝트 셋업 시점에 확정하고 `docs/infra/runbook.md` 3장에 기록한다. 현 시점의 방침은 저장소에 커밋하지 않는다는 것뿐이다.

---

---

## 5. 가용성 목표

### 5.1 등급별 목표

기능을 중요도에 따라 세 등급으로 나눈다. 모든 기능에 동일한 가용성을 요구하면 비용이 불필요하게 커진다.

등급 구분과 RTO·RPO 체계는 「행정기관 및 공공기관 정보시스템 안정성 고시」(2026.3)의 등급별 복구 목표 체계를 참조했다. 민간 서비스이므로 고시의 적용 대상은 아니며, 목표값도 해당 등급 기준보다 엄격하게 잡았다.

| 등급 | 대상 기능 | 목표 | RTO | RPO | 참조 등급 |
|---|---|---|---|---|---|
| 1등급 | PROP-01 · 02 · 03 · 08, USER-02 | 99.5% / 월 | 30분 | 5분 | A2 (3~12시간) |
| 2등급 | RISK-01, LOAN-01, NOTI-03 | 99.0% / 월 | 2시간 | 15분 | A3 (1~5일) |
| 3등급 | RISK-08 배치 | 95.0% / 월 | 24시간 | 24시간 | A4 (3주) |

**이중화 방식도 등급에 따라 다르다.** 애플리케이션 계층은 두 슬롯이 동시에 요청을 처리하는 **액티브-액티브**이고, 데이터 계층은 standby가 트래픽을 받지 않는 **액티브-스탠바이**다. 후자는 승격이라는 수동 단계를 거치므로 복구 시간이 절차 소요에 묶인다. 1등급 RTO 30분은 그 절차(`docs/infra/runbook.md` 6.1절)의 단계별 소요를 담도록 잡은 값이다. 2·3등급의 2시간·24시간은 같은 절차에 더 여유를 준 것이지 다른 수단을 쓰는 것이 아니다.

두 계층에 같은 방식을 쓰지 않는 이유는 상태의 유무다. 애플리케이션은 무상태라 어느 슬롯이 받아도 결과가 같고, 동시에 처리해도 조정할 것이 없다. 데이터베이스는 스트리밍 복제가 프라이머리 하나를 전제하므로 양쪽이 쓰기를 받으면 내용이 갈린다. standby를 복제 수신 전용으로 두고 승격을 거치는 것이 그 때문이다.

99.5%는 월 약 3시간 39분의 허용 중단에 해당한다. 계획된 배포로 인한 중단은 INF-02로 이 시간에서 제외한다.

> **집계 수단은 미정이다.** 아래 집계식은 관측 스택(INF-05)을 전제하는데 관측이 차기 범위로 빠졌다(2026-09-19). 목표값은 유지하고, 무엇으로 집계할지는 INF-03 작업에서 정해 이 절을 고친다. 그때까지 아래는 차기 설계다.

달성률은 `docs/infra/observability.md` 3장의 요청 지표로 집계한다. 30일 구간의 성공 응답 비율을 `docs/infra/runbook.md` 2장 월 1회 점검에서 확인한다.

```promql
sum(rate(http_server_requests_seconds_count{status!~"5.."}[30d]))
/
sum(rate(http_server_requests_seconds_count[30d]))
```

**RPO 5분은 INF-03(복제)가 아니라 INF-04(WAL 아카이빙 5분 강제 스위치)가 보장한다.** 두 수단의 역할을 혼동하지 않는다.

### 5.2 성능 저하 시 동작 정의

구성 요소가 정지했을 때 서비스가 어디까지 동작해야 하는지를 사전에 정의한다. 정의가 없으면 장애 시 판단이 즉흥적으로 이루어진다.

| 중단 대상 | 동작 가능 | 동작 불가 | 사용자 안내 |
|---|---|---|---|
| Redis | 매물 조회 · 상세 · 위험도 조회 | 로그인 유지, 실시간 알림 | 재로그인 요청 |
| Redis **재시작**(정지가 아니라) | 전 기능 | 없음 — 재기동까지 수초 | 없음. 토큰이 AOF로 살아남는다(3장) |
| 앱 1대 | 전 기능 | 없음 | 없음 |
| PostgreSQL Standby | 전 기능 | 없음 | 없음 |
| PostgreSQL Primary | 없음 | 전 기능 | 점검 페이지 |
| NAS-01 | 전 기능 | 없음 — 논리 백업 · 설정 사본만 멈춘다 | 없음. 물리 백업 · WAL(S3)은 계속된다 |
| NAT-01 | 전 기능 | 없음 — 사설 노드의 패키지 갱신 · 이미지 수신만 멈춘다 | 없음 |

---

## 6. 비용 산정

**미확정이다.** 아래는 배치 대상이 정해지기 전(2026년 8월)의 개략치이며 AWS EC2 기준이 아니다. 리전 · 인스턴스 유형을 1차 배포에서 확정하면(2.1절) 그 요금표를 근거로 본 절을 다시 쓴다. TLS 인증서 · 도메인 · APP-02 행은 지금 구성에 없다(2.1절 · 차기 범위).

| 항목 | 사양 | 월 비용(개략) |
|---|---|---|
| APP-01 | 2 vCPU / 2 GB — t3.small (확정, 3장) | **미확정** — 유형이 확정됐으므로 서울 리전 요금표로 산정한다 |
| DB-01 | 2 vCPU / 2 GB | 12,000 ~ 15,000원 |
| DB-02 (standby) | 1 vCPU / 1 GB | 6,000 ~ 8,000원 |
| NAS-01 · NAT-01 | 미확정 | **미확정** — 인스턴스 유형 확정 후. NAT Gateway(관리형) 대신 NAT 인스턴스를 고른 이유가 이 비용이다(서버 운영 기반 설계서 3.2) |
| 블록 스토리지 | 60 GB | 3,000 ~ 4,000원 |
| 오브젝트 스토리지(백업) | 20 GB | 500원 미만 |
| TLS 인증서 | Let's Encrypt | 0원 |
| 도메인 등록 | .com 등 | 연 15,000 ~ 20,000원 (월 환산 약 1,500원) |
| **평시 합계** | | **약 43,500 ~ 54,500원** |
| APP-02 (시험 기간 한정) | 2 vCPU / 4 GB | 일 700 ~ 800원 |

블록 스토리지 60 GB는 DB 데이터가 아니라 OS·컨테이너 이미지, 로그, WAL 아카이브 스테이징이 대부분을 차지한다. 항목별 소요량은 1주차에 스키마 적용과 더미 데이터 적재를 마친 뒤 실측해 확정한다.

**절감 수단**

- standby를 최소 사양으로 두고 승격 시점에만 사양을 올린다(재기동 필요, RTO +3분)
- 상시 이중화가 불필요한 기간에는 DB-02를 정지하고 백업 기반 복구로 낮춘다

---