# 전월세 부동산 금융 플랫폼 — 기술 스택 정의서

> 애플리케이션 기술 선정 결과 및 선정 사유
> Java 21 · Spring Boot 4.1 · JPA + MyBatis · PostgreSQL · React
>
> ※ 본 문서는 **애플리케이션 기술**만 다룬다. 배포·운영·관측·시험 기술은 `docs/infra/tech-stack.md`를 따른다.
> ※ 차기 범위 기술은 표시만 하며, 착수 순서는 `docs/roadmap.md`를 따른다.
> 작성 기준일 : 2026년 8월

## 1. 문서 목적 및 선정 원칙

본 문서는 애플리케이션에 사용할 기술을 계층별로 확정하고, 각 항목의 선정 사유와 고정 버전을 기록한다. 구현 방침과 설계 규칙은 `docs/architecture/`가 다룬다.

### 1.1 선정 원칙

- **설계 원칙 계승** — 모든 계산·판정 로직은 애플리케이션 계층에서 수행한다. 저장 프로시저·트리거로 비즈니스 로직을 옮기지 않는다.
- **1인 개발** — 러닝 커브가 크거나 운영 인력을 전제하는 기술(MSA, 메시지 브로커, 리액티브 스택)은 배제한다.
- **검증 가능성 우선** — AI 코딩 에이전트 기반으로 개발하므로, 컴파일·테스트·마이그레이션으로 정합성을 기계적으로 확인할 수 있는 구조를 택한다.
- **무상태 전제** — 두 인스턴스로 구동되므로 세션·토큰·캐시를 프로세스 메모리에 두지 않는다.

### 1.2 스택 요약

| 계층 | 채택 기술 | 핵심 근거 |
|---|---|---|
| 언어 | Java 21 (LTS) | 가상 스레드로 외부 API 병렬 호출 처리 |
| 프레임워크 | Spring Boot 4.1.x | 3.5 계열 지원 종료(2026.06.30) |
| 영속성 (엔티티) | Spring Data JPA + Hibernate 7 | 30개 테이블 매핑, 모든 변경 경로 |
| 영속성 (DTO 조회) | MyBatis 4.0.x | 화면 전달용 조회. SQL 직접 통제 |
| DB | PostgreSQL 17 | 컨테이너로 기동. 로컬·CI·운영 동일 |
| 공유 저장소 | Redis 7 | 캐시 · 토큰 · 분산 락 · 이벤트 팬아웃 |
| 배치 | Spring Batch + 스케줄러 | 등기 변동 감지 · 위험도 재분석 |
| 프론트엔드 | React + TypeScript + TanStack Query v5 | 서버 상태 캐싱으로 프론트 작업량 최소화 |
| 알림 전달 | Spring Events · SSE | 생성과 발송 분리 |
| 알림 수신 | EventSource | 수신 시 캐시 무효화로 처리 |
| 테스트 | JUnit 5 · AssertJ · Testcontainers | 판정 로직 검증 + 실제 DB 통합 테스트 |
| 횡단 관심사 | Spring AOP | 로깅 · 측정 · 분산 락 · 이력 적재 분리 |
| 지표 노출 | Actuator + Micrometer | 수집은 인프라가 담당 |
| 외부 연동 격리 | RestClient + Resilience4j | 타임아웃 · 재시도 · 서킷 · 폴백 |
| 로컬 환경 | Docker Compose (PostgreSQL · Redis) | 호스트 직접 설치 없이 명령 한 줄로 구동 |

---

## 2. 백엔드

| 기술 | 버전 | 선정 사유 |
|---|---|---|
| Java | 21 (LTS) | **가상 스레드(JEP 444)** — 매물 1건 분석 시 등기·건축물대장·시세 API를 동시에 호출해야 한다. 리액티브 스택 없이 블로킹 코드 그대로 동시성을 확보할 수 있어 1인 개발에서 학습 비용 대비 효과가 가장 크다. **Record** — 요청·응답 DTO의 보일러플레이트 제거. **봉인 인터페이스 + switch 패턴 매칭** — 위험 등급(SAFE·CAUTION·DANGER)과 보증기관 3사 판정 결과를 타입으로 표현하면 분기 누락이 컴파일 시점에 잡힌다. 판정 로직이 프로젝트의 핵심이므로 이 안전장치의 가치가 크다. **텍스트 블록** — 네이티브 쿼리 가독성 |
| Spring Boot | 4.1.x | 3.5 계열이 2026년 6월 30일자로 오픈소스 지원이 종료되어 3.x 신규 채택 근거가 없다. 4.1은 Spring Framework 7 · Spring Security 7 · Hibernate 7 기반이며 2027년 7월까지 지원된다. 최소 요구는 Java 17이나 가상 스레드 활용을 위해 21로 구동한다 |
| Spring Web MVC | — | WebFlux 대신 MVC를 선택한다. 가상 스레드로 동시성 요구가 해소되므로 리액티브의 학습 비용을 감수할 이유가 없다. JPA가 블로킹 기반인 점도 MVC 쪽이 정합적이다 |
| Spring Data JPA + Hibernate 7 | — | 알림(NOTIFICATION + 하위)과 보증보험 기준(GUARANTEE_CRITERIA + HUG/HF/SGI)이 배타적 슈퍼타입-서브타입이므로 JOINED 상속 전략으로 설계 의도를 코드에 그대로 반영한다. **엔티티를 반환하는 모든 경로를 담당한다** — 등록·수정·삭제, 변경 대상 로딩, 연관 탐색 |
| MyBatis | 4.0.x | **화면에 전달할 데이터를 반환하는 조회를 담당한다.** 매물 검색의 동적 조건 조합은 동적 SQL로, 순위번호 기준 선순위채권 합산과 자치구 집계는 SQL을 직접 작성해 실행 계획을 통제한다. 경계는 반환 타입으로 나눈다 — 엔티티가 필요하면 JPA, 화면 전달용이면 MyBatis. 역할 분담은 `docs/architecture/persistence.md`를 따른다. **4.0.x가 Spring Boot 4 대응 라인이다** |
| Spring Security + JWT | — | 액세스·리프레시 토큰 발급과 회전. 토큰은 Redis에 보관한다. `USER_AUTH`의 `auth_type`·`provider_id` 컬럼은 소셜 로그인 확장 지점으로 남긴다 |
| RestClient | Spring Framework 7 | 외부 API 연동용. 동기식이지만 가상 스레드 위에서 동작하므로 병렬 호출에 문제가 없고 WebClient 대비 코드가 단순하다. **연동 대상별로 인터페이스를 두고 Mock·Real·Fault 세 구현을 둔다** |
| Resilience4j | — | 등기 API 한 곳의 장애가 매물 조회 전체를 멈추게 해서는 안 되므로 타임아웃·재시도·서킷 브레이커와 대상별 폴백을 적용한다. 서킷 상태는 커스텀 지표로 노출한다 |
| Spring Batch + `@Scheduled` | — | 등기 변동 감지 → `RISK_ANALYSIS` 재계산 → 등급 변동 시 알림 생성이 전형적인 배치 파이프라인이다. **두 인스턴스에서 중복 실행되므로 Redis 분산 락으로 단일 실행을 보장한다** |
| Spring Events | — | 알림 생성을 판정·저장 트랜잭션에서 분리한다. **트랜잭션 커밋 이후에 발행한다** — 커밋 전에 발행하면 롤백된 판정의 알림이 나간다 |
| SSE (Server-Sent Events) | — | 알림은 서버에서 클라이언트로 향하는 단방향 전달이다. WebSocket보다 구현과 재연결 처리가 단순하고 HTTP 기반이라 프록시 설정 부담이 없다. **인스턴스가 둘이므로 Redis Pub/Sub으로 전 인스턴스에 브로드캐스트하고, 각 인스턴스가 해당 연결 보유 여부를 확인해 전달한다** |
| Flyway | — | 30개 테이블 스키마를 형상 관리 대상으로 둔다. DDL을 단일 진실 원천으로 고정해야 엔티티가 설계서에서 이탈하지 않는다. `hibernate.ddl-auto`는 `validate`로 고정한다. **롤링 배포 중 신·구 버전이 공존하므로 마이그레이션은 하위 호환이어야 한다** |
| Bean Validation | — | 보증금·연소득·신용점수 등 입력값 제약을 컨트롤러 진입 시점에 차단한다 |
| springdoc-openapi | — | 1인 개발이라 API 문서를 수기로 유지할 여력이 없다. 코드에서 자동 생성한다 |
| Spring AOP | — | 실행 시간 측정, 외부 API 호출 로깅, 분산 락, 이력 적재를 관점으로 분리한다. 상세는 `docs/architecture/spring-aop.md`를 따른다 |
| Spring Cache + Redis | — | 외부 API 조회 결과와 자치구 집계를 캐싱한다. 애노테이션 기반이라 서비스 코드 변경 없이 적용·해제가 가능하다 |
| Actuator + Micrometer | — | 지표 노출. 수집·시각화·알림은 인프라가 담당한다. **SSE 활성 연결 수와 서킷 상태는 커스텀 지표로 직접 노출한다** |
| Lombok | — | 엔티티 보일러플레이트 축소. 단 `@Data`·`@Setter`는 엔티티에 사용하지 않는다 |

**차기 범위** — Spring AI 2.0.x(CONS 상담), OAuth2 Client(소셜 로그인), Firebase Cloud Messaging(NOTI-04 웹 푸시). 발송 채널은 인터페이스로 추상화하고 Mock 구현을 두어 구현체 교체만으로 동작하게 한다.

---

## 3. 데이터 계층

| 기술 | 버전 | 선정 사유 |
|---|---|---|
| PostgreSQL | 17 | 컨테이너로 기동해 로컬 개발부터 운영까지 동일 환경을 유지한다. Testcontainers 연동이 간단해 통합 테스트를 실제 DB로 돌릴 수 있다. 타입 표기와 식별자 채번 규약은 `docs/architecture/database.md`를 따른다 |
| Redis | 7 | 외부 API 호출량 절감(캐시), 리프레시 토큰 보관, 알림 중복 발송 방지 키, 배치 분산 락, SSE 이벤트 팬아웃. **두 인스턴스가 같은 상태를 보게 하는 유일한 수단이다** |
| Flyway 마이그레이션 | — | `V1__init_schema.sql`에 30개 테이블 DDL을 정의하고, 시드 데이터(보증보험 3사 기준, 대출 규제 수치, 공통 코드)는 별도 마이그레이션으로 분리한다 |

---

## 4. 프론트엔드

| 기술 | 버전 | 선정 사유 |
|---|---|---|
| React + TypeScript | — | 판정 결과·대출 한도 등 숫자 계약이 많은 화면이라 타입 검증의 이득이 크다. 백엔드 DTO와 타입을 대응시켜 연동 오류를 컴파일 시점에 차단한다 |
| Vite | — | 빌드 설정 부담이 적어 백엔드 작업 시간을 침범하지 않는다 |
| TanStack Query | v5 | 매물 검색 필터 조합별 캐싱, 지도 이동 시 이전 결과 유지(`placeholderData`), 관심 매물 등록 후 목록 자동 무효화를 표준 방식으로 처리한다. 서버 상태를 전담시키면 별도 전역 상태 관리 라이브러리가 불필요해진다. 쿼리 키는 팩토리로 중앙 관리해 매물–위험도 간 무효화 연쇄를 일관되게 유지한다 |
| EventSource | 브라우저 내장 | 단방향 알림 수신. 별도 라이브러리 없이 표준 API로 처리한다. **연결은 로그인 이후 한 번만 수립하고 애플리케이션 최상위에서 관리한다** — 화면마다 열면 중복 수신과 연결 누수가 발생한다. 배포 중 슬롯 교체로 끊기므로 재연결 후 목록을 재조회한다 |
| 카카오맵 SDK | — | 매물 위치 표시와 반경 검색 UI. 국내 주소 체계 대응이 가장 안정적이다 |

**차기 범위** — Firebase JS SDK + 서비스 워커(NOTI-04 웹 푸시), Recharts(시세 추이 차트 · 상환 시뮬레이션).

---

## 5. 개발 환경

| 기술 | 버전 | 선정 사유 |
|---|---|---|
| Docker Compose | — | PostgreSQL과 Redis를 컨테이너로 구동한다. **로컬 호스트에 데이터베이스를 직접 설치하지 않는 것을 원칙으로 한다.** 버전 고정으로 로컬·CI·운영의 DB 버전 불일치를 제거하고, 명령 한 줄로 환경을 초기화할 수 있어 스키마를 반복 재생성하는 초기 단계에 유리하다 |
| Testcontainers | — | 통합 테스트의 기본 실행 환경. 개발 환경에 Docker가 이미 전제되므로 추가 설치 비용이 없다. H2는 방언 차이로 통과와 실패가 어긋나 사용하지 않는다 |

운영 환경 구성과 배포·관측·시험 도구는 `docs/infra/tech-stack.md`를 따른다.

---

## 6. 연동 외부 API

| 용도 | 제공처 | 적재 대상 · 처리 방식 | 범위 |
|---|---|---|---|
| 건축물대장 | 국토교통부 (공공데이터포털) | `BUILDING_LEDGER` — 위반건축물 여부·용도·면적 | 1단계 |
| 전월세 실거래가 | 국토교통부 (공공데이터포털) | `PROPERTY.market_price` | 1단계 |
| 주소 정규화·좌표 | 도로명주소 API + 카카오 로컬 | `PROPERTY` 좌표. 등기 주소와 대장 주소 일치 판정의 기준 | 1단계 |
| 부동산 등기부등본 | 대법원 인터넷등기소 | `BUILDING_REGISTRY`, `OWNERSHIP_HISTORY`, `MORTGAGE_HISTORY` — **무료 공개 API가 없고 유료 중계만 존재하므로 Mock 어댑터로 구현한다** | 1단계 |
| 보증보험 3사 기준 | HUG · HF · SGI 공시 자료 | `GUARANTEE_CRITERIA` 및 서브타입 — 개방 API 부재. 시드 적재 후 ADMIN-01로 갱신 | 1단계 |
| 대출 상품·금리 | 금융감독원 금융상품 비교공시 | `LOAN_PRODUCT` | **차기 (LOAN-03)** |
| 기준금리·시장금리 | 한국은행 ECOS | `INTEREST_RATE_HISTORY` | **차기 (LOAN-06)** |
| 소셜 로그인 | 카카오 · 네이버 OAuth 2.0 | `USER_AUTH` | **차기** |
| 푸시 발송 | Firebase Cloud Messaging | 발송 이력 | **차기 (NOTI-04)** |
| AI 상담 | Gemini API | `CONSULT_MESSAGE` | **차기 (CONS)** |

**실연동 4종은 1일차에 사용 신청한다.** 승인 대기는 작업량으로 줄지 않으며, 이 일정에서 유일하게 압축 불가능한 항목이다. 승인 전에는 Mock 어댑터로 진행한다.

---

## 7. 버전 고정

| 항목 | 고정 버전 | 비고 |
|---|---|---|
| Java | 21 (LTS) | 장기 지원 버전 |
| Spring Boot | 4.1.x | 2027년 7월까지 지원 |
| MyBatis Spring Boot Starter | 4.0.x | 3.0.x는 Boot 3 전용 |
| PostgreSQL | 17 | `docs/infra/tech-stack.md`와 동일 |
| Redis | 7 | `docs/infra/tech-stack.md`와 동일 |
| TanStack Query | v5 (React) | v4 문법 혼용 금지 |
| Node.js | 22 LTS | 프론트엔드 빌드 |
| Docker 이미지 | `postgres:17-alpine` · `redis:7-alpine` | 로컬·CI·운영 공통 고정 |