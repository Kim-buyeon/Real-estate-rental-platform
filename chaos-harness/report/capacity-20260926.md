# 용량 산정 리포트 (2026-09-26)

시험 계획서 7.1 양식. 판정 기준은 시험 계획서 2.3, 부하 프로파일은 트래픽 정의서, 스크립트는 `chaos-harness/load/`. 경과는 이슈 #257(시험) · #258(과부하 결함)에 있다.

## 시험 조건

| 항목 | 값 |
| --- | --- |
| 대상 사양 | t3.small(2 vCPU · 2 GiB) 4대 — APP-01(입구 Nginx · 앱 슬롯 둘 · Redis · 수집기 · NAT), APP-02(앱 슬롯 둘), DB-01(primary), DB-02(standby). 앱 슬롯 넷 각각 `mem_limit` 600m · 힙 60% · Tomcat 스레드 50 · Hikari 8 |
| 데이터 규모 | `property` 67,183건 · `mortgage_history` 9,835건(매물당 약 0.15건 — 트래픽 정의서 6장의 「매물당 8건」에 **못 미친다**, 한계 1) |
| 부하 생성기 | LOAD-01 — t3.small, 같은 VPC 사설 서브넷(2a), 대상은 APP-01 앞단 사설 주소 `10.20.0.10` |
| 애플리케이션 커밋 | `d2928b8`(백엔드 · 프론트 이미지). 앞단 설정은 측정마다 아래 「측정 회차」 |
| 도구 | k6 v2.3.0. 단계 집계는 `chaos-harness/load/tools/stages.py`(워밍업 60초 제외) |

**측정 회차**

| 회차 | 시각 | 앞단 요청 상한 | SSE 250 연결 | 비고 |
| --- | --- | --- | --- | --- |
| T1 | 16:44 ~ 16:54 | 없음 | 유지 | 기준선 10 RPS · think time 켬 |
| T2-① | 16:55 ~ 17:29 | 없음 | 유지 | 200 RPS 에서 붕괴 → APP-01 OS 정지 약 70분 |
| T2-② | 20:43 ~ 21:17 | 100 r/s · burst 50 | **없음**(한계 2) | 개선 전 |
| T2-③ | 21:28 ~ 22:01 | **60 r/s · burst 30** | **없음** | 개선 후. ②와 조건 동일 |

T2 는 모두 `THINK=0`(think time 끔) — 켜면 200 RPS 에서 VU 최대 4,200 으로 생성기 메모리가 모자란다(#257 계획). 목표 RPS 는 arrival-rate 로 고정되므로 처리량 비교에는 영향이 없고, 동시 세션 수는 실제보다 작다.

## 단계별 결과

**T1 기준선** — 10 RPS, 전체 p95 102.7ms · p99 184.7ms · 5xx 0 · 검증 100%. 가장 무거운 지도 2단계 p95 199ms.

**T2-② 개선 전**(상한 100 r/s). 「받아들인」은 2xx · 3xx 응답만 센 것 — 429(상한 거절) · 401(토큰 풀, 한계 3)은 뺐다. 자원은 10초 간격 기록의 단계 구간(워밍업 뒤) 평균 / 최대.

| 목표 RPS | 받아들인 RPS | p95 | p99 | 5xx · 무응답 | 지도 2단계 p95 | CPU APP-01 | CPU APP-02 | CPU DB-01 | Hikari pending 최대 | 캐시 적중률 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 10 | 10.0 | 119.5 | 206.1 | 0 | 221.1 | 0.14 / 0.19 | 0.13 / 0.20 | 0.07 / 0.14 | 0 | 미측정 |
| 25 | 25.0 | 117.3 | 229.9 | 0 | 260.0 | 0.22 / 0.27 | 0.27 / 0.36 | 0.14 / 0.20 | 0 | 미측정 |
| 50 | 50.0 | 152.9 | 301.5 | 0 | 338.2 | 0.28 / 0.37 | 0.49 / 0.71 | 0.31 / 0.39 | 0 | 미측정 |
| 100 | 100.0 | 271.3 | 904.2 | 0 | **642.1** | 0.53 / 0.68 | 0.57 / 0.79 | **0.70 / 0.89** | 0 | 미측정 |
| 200 | **36.6** | **4,254** | 6,834 | **502 9,286 · 500 179 · 무응답 4,953** | 5,443 | **0.98 / 1.00** | 0.75 / 1.00 | 0.39 / 0.86 | **40** | 미측정 |

200 RPS 단계 21:16:27 ~ 28 에 **앱 슬롯 넷이 모두 컨테이너 메모리 상한(600m)에서 커널에 종료됐다**(`Memory cgroup out of memory: Killed process … java`, anon-rss 약 602 MB). 502 는 그 사이 슬롯이 없어서 났다. 노드 자체는 살아 있었다 — 첫 측정(T2-①)의 OS 정지와 다른 점이다(#258 의 kdump 예약 해제 · 요청 상한).

**T2-③ 개선 후**(상한 60 r/s)

| 목표 RPS | 받아들인 RPS | p95 | p99 | 5xx · 무응답 | 지도 2단계 p95 | 429 | CPU APP-01 | CPU APP-02 | CPU DB-01 | Hikari pending 최대 | 캐시 적중률 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 10 | 10.0 | 116.7 | 191.5 | 0 | 204.5 | 0 | 0.14 / 0.18 | 0.15 / 0.23 | 0.08 / 0.15 | 0 | 미측정 |
| 25 | 25.0 | 107.7 | 215.9 | 0 | 250.5 | 0 | 0.21 / 0.28 | 0.22 / 0.37 | 0.16 / 0.23 | 0 | 미측정 |
| 50 | 50.0 | 131.7 | 253.5 | 0 | 294.0 | 2 | 0.30 / 0.39 | 0.33 / 0.44 | 0.31 / 0.45 | 0 | 미측정 |
| 100 | 60.0 | 142.8 | 272.7 | 0 | 302.2 | 9,605 | 0.30 / 0.37 | 0.41 / 0.54 | 0.40 / 0.47 | 0 | 미측정 |
| 200 | 54.5 | 147.9 | 264.4 | 0 | 285.7 | 33,603 | 0.36 / 0.42 | 0.28 / 0.40 | 0.40 / 0.56 | 0 | 미측정 |

200 RPS 단계의 받아들인 RPS 가 60 에 못 미치는 것은 토큰 풀 401(1,322건)이 상한 안의 자리를 먹었기 때문이다(한계 3). 앱 슬롯 종료 0, `dropped_iterations` 0.

## 포화점

| 항목 | 값 |
| --- | --- |
| 실측 한계 | **50 RPS 는 통과, 100 RPS 에서 지도 2단계 p95 642ms**(기준 500ms 초과). 둘 사이의 값은 재지 않았다 — 단계가 50 → 100 이라 「몇 RPS 에서 512ms」처럼 값으로 특정하지 못했다(시험 계획서 2.3 이 요구하는 정밀도에 못 미친다) |
| 먼저 한계에 닿은 것 | **엔드포인트는 지도 2단계**(`GET /api/properties` 바운딩 박스). 자원은 100 RPS 에서 **DB-01 CPU**(평균 0.70 · 최대 0.89)가 앱 노드(0.53 · 0.57)보다 먼저 올랐다 |
| 붕괴 양상 | 상한이 받는 양이 한계를 넘으면(T2-② 200 RPS) 앱 슬롯이 CPU(APP-01 1.00)와 **메모리**(컨테이너 상한 종료)로 함께 무너진다. Hikari pending 40 은 원인이 아니라 CPU 기아의 결과로 본다 — 같은 단계 APP-02 는 0 이었다 |
| 판정 근거 수치 | 지도 2단계 한 번이 자치구 하나의 매물을 **전량** 돌려준다 — 강서구 6,003건 · 응답 1.27 MB(압축 전) · 부하 없을 때 0.30 ~ 0.40초. 그중 DB 실행은 약 50ms(`EXPLAIN ANALYZE`, 좌표 인덱스 · 최신 분석 인덱스 사용, 저당 조회는 해시 서브플랜 1회)이고 **나머지가 앱의 결과 매핑 · 직렬화**다 |

## 용량 판단

| 항목 | 판단 |
| --- | --- |
| 실측 한계 RPS | 50 RPS 이상 100 RPS 미만(위 포화점). 앞단 상한은 60 r/s 로 뒀다 |
| 현재 헤드룸 | 예상 피크 8 RPS(트래픽 정의서) 대비 **6배 이상**(50 RPS 기준) |
| 증설 불필요 구간 | 트래픽 정의서의 DAU ↔ 피크 RPS 환산을 그대로 쓰면 50 RPS 는 예상 피크의 6.25배다 — 그 배수만큼의 DAU 까지. 환산식 자체가 가정이라(트래픽 정의서) DAU 숫자로 적지 않는다 |
| 증설 권고 방향 | **증설보다 지도 2단계 응답 크기가 먼저다.** 한계에 먼저 닿은 것이 한 엔드포인트의 전량 응답이고, 그 비용 대부분이 앱 쪽 매핑 · 직렬화라 vCPU 나 노드를 늘려도 요청당 비용은 그대로다. 서버가 반환 건수에 상한을 두거나 서버에서 묶어 보내는 쪽 — API 명세 · 화면의 묶음 표시(현재 화면이 격자로 묶는다)를 함께 바꾸는 일이라 이번에 하지 않았다. 그다음이 DB-01 CPU(100 RPS 에서 먼저 오른 자원) |
| 축소 가능성 | 없다. 개선 후 60 r/s 에서 CPU 는 여유가 있으나(최대 0.56), **메모리 여유가 없다** — APP-01 가용 메모리 최소 145 MB, APP-02 104 MB. 2 GiB 아래로 내리면 앱 슬롯 둘이 들어가지 않는다 |
| 알림 임계 조정 | 미검증 알림 임계(시험 계획서 2.4 의 이 항목)는 **이번에 고치지 않는다.** 60 r/s 상한 아래에서는 CPU · Hikari 가 임계 근처에 가지 않았다. 새로 생긴 신호는 **429 비율**이다 — 상한에 닿기 시작했다는 뜻이라 증설 · 개선 시점의 선행 지표가 된다. 앞단 exporter 는 상태 코드를 세지 않아(stub_status) 지금 수단으로는 못 본다 — 로그 전송(Vector) 쪽에서 세는 것이 후속이다 |

## 개선 전후 비교

T2-②(개선 전)와 T2-③(개선 후)은 앞단 상한 값 말고는 조건이 같다 — 같은 날 같은 노드 · 같은 이미지 · 같은 스크립트 · 같은 토큰 풀 크기 · SSE 없음.

| 항목 | 개선 전 | 개선 후 | 조치 내용 |
| --- | --- | --- | --- |
| 200 RPS 단계 5xx · 무응답 | 502 9,286 · 500 179 · 무응답 4,953 | **0** | 앞단 요청 상한 100 → 60 r/s, burst 50 → 30(#258) |
| 200 RPS 단계 받아들인 요청 p95 | 4,254 ms | **147.9 ms** | 〃 |
| 200 RPS 단계 받아들인 RPS | 36.6 | **54.5** | 〃 — 거절을 늘렸는데 성공은 늘었다 |
| 앱 슬롯 종료(cgroup OOM) | 넷 모두 | **0** | 〃 |
| 100 RPS 단계 지도 2단계 p95 | 642.1 ms | 302.2 ms | 〃 — 대신 이 단계에서 40%(9,605건)를 429 로 거절한다 |
| 과부하 뒤 노드 | (T2-①) APP-01 OS 정지 약 70분 · 스스로 회복 못 함 | 노드 정지 없음 | kdump 메모리 예약 해제 · 커널 갱신 뒤 재발 방지, 상태 검사 경보의 자동 재부팅 · 복구(#258) |
| Redis 없을 때 앱 기동 | (T2-①) APP-02 슬롯 재시작 60회 | 기동 뒤 구독 재시도 | 알림 구독 시작을 비동기 재시도로(#259) |

**이 개선은 한계를 올리지 않았다.** 상한은 넘치는 부하를 거절해 받아들인 요청의 품질과 노드를 지킬 뿐이다. 100 RPS 를 실제로 처리하려면 위 증설 권고 방향의 지도 2단계 응답 크기를 먼저 고쳐야 한다.

## 한계

1. **데이터 규모가 정의에 못 미친다.** 등기 이력(`mortgage_history`)이 매물당 약 0.15건으로, 트래픽 정의서 6장의 매물당 8건의 2% 수준이다. 지도 조회의 저당 확인은 해시 서브플랜이라 영향이 작다고 보지만, 위험도 조회(`GET …/risk` — 조회 시점에 분석한다) 비용은 이력이 늘면 오른다. 실측하지 않았다.
2. **T2-② · ③ 에는 SSE 250 연결이 없다.** k6 자동 확장 해석이 `k6/x/sse` 를 받지 못해 SSE 스크립트가 곧바로 끝났는데, 본 프로파일은 그대로 돌았다(하네스 README 에 함정으로 적었다). 첫 측정 T1 · T2-① 은 연결을 유지했다. 개선 전후 비교는 둘 다 없는 조건끼리라 성립하지만, **SSE 연결이 있는 상태의 60 r/s 는 재지 않았다.** 연결 250개의 메모리 몫이 앱 슬롯 상한을 더 좁힐 수 있다.
3. **토큰 풀(100) < VU.** `THINK=0` 이어도 200 RPS 단계는 VU 가 풀보다 많아, 30분 뒤 소유자가 아닌 VU 의 인증 요청이 401 이 된다(하네스 README 한계 1). 서버 문제가 아니지만 상한 안의 자리를 먹는다.
4. **포화점을 값으로 특정하지 못했다**(포화점 절). 50 · 60 · 75 · 100 처럼 촘촘한 단계로 한 번 더 재야 한다.
5. **캐시 적중률 · GC 일시정지 · 힙과 힙 밖의 구분을 재지 않았다.** 앱 슬롯 종료가 힙이 아니라 컨테이너 상한에서 났으므로(힙 상한 360m, 종료 시 RSS 약 602 MB) **힙 밖이 240m 을 넘었다는 것까지만 안다** — 무엇이 늘었는지는 모른다(시스템 구성서 한계 항목).
6. **표본 1회.** 같은 조건을 반복하지 않았다. 60 r/s 는 잠정값이다.
7. **T3 지속 · T4 누수 · T5 캐시 콜드 · T6 · T7 배치 동시는 돌리지 않았다.** T4 는 2시간 · 비용, T5 는 `FLUSHALL` 이 로그인 토큰까지 지운다, T7 은 배치를 기동할 수단을 확인하지 못했다(#257 계획).
8. **실사용 패턴이 아니다.** 요청 조합 · 지역 분포 · 지도 영역 크기는 트래픽 정의서의 가정이고, 지도 2단계 영역 반폭은 스크립트가 스스로 정한 값이다(`lib/mix.js`). 실제 화면이 더 좁은 영역을 부르면 응답이 작아진다.
9. **생성기의 응답 압축.** k6 는 압축을 요청하지 않아 지도 2단계를 1.27 MB 그대로 받았다(앞단 기록 — 같은 요청을 압축으로 받으면 106 KB). 브라우저는 압축을 요청하므로 실사용에서는 앞단의 압축 CPU 가 들고 전송량이 준다. 이 차이는 재지 않았다.
